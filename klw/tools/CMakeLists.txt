  iree_cc_binary(
    NAME
      klw-compile
    SRCS
      "klw-compile-main.cc"
    DEPS
      iree::klw::compiler::Tools::klw_compile_lib
    DATA
      ${IREE_LLD_TARGET}
    HOSTONLY
  )

  iree_cc_binary(
    NAME
      klw-opt
    SRCS
      "klw-opt-main.cc"
    DEPS
      LLVMSupport
      MLIRIR
      MLIROptLib
      MLIRSupport
      iree::compiler::Tools::init_passes_and_dialects
      iree::compiler::Tools::init_targets
      iree::klw::compiler::Tools::init_klw_passes_and_dialects
    DATA
      ${IREE_LLD_TARGET}
    HOSTONLY
  )

